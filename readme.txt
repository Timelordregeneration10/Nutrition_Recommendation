界面：
    目前全部完成界面的只是登录界面和首页界面。

    数据推荐和日志界面还有美化没完成，根据后续需要界面也可能会改变。

    更多和设置界面还没想好。

前后端交互：
    约定状态码为200则为成功。
    
    登陆注册：
        在js/login.js中第28行起，若状态码为200则登陆成功，进入主界面，测试时，可以账号输入Nicholas，密码输入rmtyyds绕过服务器验证直接进入主界面
        在js/login.js中第57行起，注册时需要验证是否是合法的账号，包括用户名是否被占用，密码是否过于简单等,状态码为200则注册成功

    数据推荐：
        在js/dataRec.js中第38行，是传给后端的包含大致状况，具体症状，治疗方案的json
        在js/dataRec.js中第41行，改成接受“数据推荐”数据的地址
        在js/dataRec.js中第45行起的部分，假设服务器的响应是用 JSON 格式编写的，且json中键为recommendation的值是我要的值
    
    日志推荐：
        在js/dateRec.js中第22行，是传给后端的日志个数，食物数组，日期数组的json
        在js/dateRec.js中第25行，改成接受“数据推荐”数据的地址
        在js/dateRec.js中第29行起的部分，假设服务器的响应是用 JSON 格式编写的，且json中键为recommendation的值是我要的值
        
    设置界面：
        未完待续